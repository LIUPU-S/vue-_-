/* eslint-disable prettier/prettier */
const state = {
  count:10
};
const getters = {};
const mutations = {};
const actions = {};
export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions,
};
